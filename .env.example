# Use the existing CRM PostgreSQL connection. Fresh records use the isolated closing_desk schema.
DATABASE_URL=
# Copy the existing AUTH_SECRET, or provision a new strong secret in Vercel.
AUTH_SECRET=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_REGION=auto
S3_ENDPOINT=
S3_FORCE_PATH_STYLE=false
